class Solution:
    def findMin(self, nums: List[int]) -> int:
        l,r =0,len(nums)-1
        for i in range(len(nums)):
            if nums[r] < nums[l]:
                r = r-1
            else:
                if r!= len(nums) -1:
                    return nums[r+1]
                else:
                    return nums[0]
                




        
